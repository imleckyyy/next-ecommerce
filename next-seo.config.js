const title = "Domyślny tytuł testowego sklepu";
const description = "Domyślny opis testowego sklepu";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    siteName: "Nasz sklep",
  },
};

export default nextSeoConfig;
